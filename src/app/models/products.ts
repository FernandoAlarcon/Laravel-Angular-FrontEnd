import { Title } from "@angular/platform-browser";

export interface Products {
    id?: string,
    nombre?: string,
    descripcion?: string,
    peso?:string,
    precio?:string,
    categoria?:string
};
 
